역직렬화 왜하냐?
->json은 문자데이터라 로직못돌려서 변환 해야함
--------------I---------------------
프록시객체 내부가 진짜 객체로 채워짐
------------
TCP/IP
->“TCP/IP는 인터넷과 네트워크에서 데이터를 신뢰성 있게 전송하기 위한 프로토콜(규약) 모음”
->“백엔드는 TCP/IP 규칙을 직접 다루지 않고, OS가 그 규칙을 구현한 네트워크 스택을 통해 통신한다.”

TCP/IP접속한다 의미
->OS에게 소켓 열라는 뜻.
그러면 소켓 열고 TCP 3Way Handshake도 함.
-----------------
Nginx, ALB
->네트워크층 프록시 이다.