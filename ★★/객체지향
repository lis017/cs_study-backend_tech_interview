DI와 연결고리 관계
->DI는 연결강도를 낮춰준다.
연결고리를 줄이는게 아니다.

DI예시
-> 인터페이스 + 구현체
------------
mutable immutable 에 대해
->mutable은 생성후 상태변경 가능한 객체
immutable은 생성후 상태가 변하지않는 객체
->가변객체. 불변객체임.
-> immutable은 Thread-Safe하다.
자바 백엔드에서 선호된다
캐싱에 안전
-> immutable은 동시성에 강점
-> mutable은 성능에 강점(자주 변하는 Entity등)