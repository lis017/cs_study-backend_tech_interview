Ioc란
->IoC (Inversion of Control)
객체 생성·생명주기·의존성 관리의 제어권이 개발자 → Spring 컨테이너로 역전되는 것

-----------
웹 요청 흐름도
->클라이언트
 → 필터(Filter)
 → DispatcherServlet (누가 처리할지 결정)
 → HandlerMapping
 → HandlerAdapter (어떻게 호출할지 책임)
 → 인터셉터(preHandle)
 → 컨트롤러(Handler)
 → 인터셉터(postHandle / afterCompletion)
 → ViewResolver
 → 응답
--------------
Dispatcher servlet
->“DispatcherServlet은 핸들러를 직접 호출하지 않고, HandlerAdapter를 통해 호출 방식을 추상화합니다.”