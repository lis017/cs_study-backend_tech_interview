entity에 Countrycode가 LAZY야.
그럼 그거를 LAZY객체라고 하는건 좀 이상하고
Countrycode필드에는 LAZY로딩을 위한 프록시 객체가 들어있다. 라고하래

LAZY객체란
LAZY를 포함한 ENTITY객체가 아닌
엔티티객체에 있는 LAZY필드에 있는 프록시객체를 말함

네트워크에 보낼수있는 객체란?
->메모리에 있는 객체를 직렬화 할수 있는것.

네트워크 데이터를 dto로 받는순간 이미 Jacson이 역직렬화 내부적으로 해준것

dto를 네트워크에 반환하면 spring이 자동으로 직렬화해줌
------------
Redis 분산락 왜 필요?
->서버가 여러 대일 때 동일 자원에 대한 중복 처리를 막기 위해
->재고차감 등
->DB Unique로도 방어 가능하지만,
애플리케이션 레벨에서 제어 필요
DB 부하 감소 필요할때 사용 